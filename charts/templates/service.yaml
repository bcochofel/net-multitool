apiVersion: v1
kind: Service
metadata:
  name: {{ include "net-multitool.fullname" . }}
  labels:
    {{- include "net-multitool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "net-multitool.selectorLabels" . | nindent 4 }}
